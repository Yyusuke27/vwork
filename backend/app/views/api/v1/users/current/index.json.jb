json = {
  :success => true,
  :user => nil,
  :unread => unread_count,
  :profile => nil,
  :owner => is_owner
}

json[:user] = {
  :id => user.id,
  :name => user.name,
  :email => user.email,
  :registration => user.registration,
  :role => user.role
}

if profile.present?
  json[:profile] = {
    :position => profile&.position
  }
end

json
